export default ({ app, store }) => {
  const router = app.router

  // router.beforeEach((to, from, next) => {
  //   store.commit('SET_LAST_SCROLL_TOP', window.pageYOffset)
  //   next()
  // })

  // router.afterEach((to, from) => {
  //   store.dispatch('scrollToNav')
  // })
}
